services:
  postgres:
    image: postgres:16-alpine
    container_name: emet_postgres
    restart: always
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      TZ: Europe/Kyiv
      PGTZ: Europe/Kyiv
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  emet-bot:
    build: .
    container_name: emet_bot_app
    restart: always
    stop_grace_period: 10s
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - TZ=Europe/Kyiv
    volumes:
      - ./data:/app/data
      # credentials.json більше не монтується — використовуємо GOOGLE_SERVICE_ACCOUNT_JSON у .env
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f 'python main.py' > /dev/null || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    depends_on:
      postgres:
        condition: service_healthy

  emet-admin:
    build: .
    container_name: emet_admin_panel
    restart: always
    stop_grace_period: 10s
    command: python admin_panel.py
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - TZ=Europe/Kyiv
    volumes:
      - ./data:/app/data
    ports:
      - "5000:5000"
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
